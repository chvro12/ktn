# Remonte toutes les dépendances à la racine node_modules/
# Nécessaire pour que `node dist/main.js` trouve ses packages dans Docker
# sans avoir à copier chaque workspace/node_modules individuellement.
shamefully-hoist=true
